# ローカルで開く方法

Windowsでは `start-local.bat` をダブルクリックしてください。
ブラウザーで `http://127.0.0.1:8000/` が開きます。

終了するときは、起動した黒い画面を閉じるか、その画面で `Ctrl+C` を押してください。

コマンドから起動する場合は、このフォルダーで次を実行します。

```powershell
node local-preview-server.mjs
```
